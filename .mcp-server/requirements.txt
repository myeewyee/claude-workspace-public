# Phase 1: Core (keyword search, graph, browse)
fastmcp>=2.0,<3.0
rank-bm25>=0.2.2
rapidfuzz>=3.0
python-frontmatter>=1.1.0

# Phase 2: Semantic search
lancedb>=0.17
sentence-transformers>=3.0

# Phase 3: Session awareness (no additional packages needed)
